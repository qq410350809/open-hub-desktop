{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permissions for the local OpenHub interface.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
