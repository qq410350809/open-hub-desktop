{
  "name": "open-hub-desktop",
  "private": true,
  "version": "0.3.0",
  "description": "Local website library built with Tauri and SQLite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "desktop": "tauri dev",
    "desktop:build": "tauri build",
    "tauri": "tauri",
    "clean:target": "bash scripts/clean-target.sh",
    "clean:deep": "bash scripts/clean-target.sh --deep",
    "postdesktop": "bash scripts/clean-target.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.2.0"
  },
  "dependencies": {
    "@tanstack/vue-table": "^9.1.2",
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-opener": "^2.5.0",
    "echarts": "^6.1.0",
    "vue": "^3.5.13"
  }
}
